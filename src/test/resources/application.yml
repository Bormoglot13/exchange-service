grpc:
  server:
    port: 7080
  client:
    port: 7080
    __name__:
      adress: static://localhost:7080
    channelShutdownTimeoutMs: 3000


spring:
  main:
    banner-mode: off

logging:
  level:
    root: warn
    pro.utorg: debug
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %file:%line %msg%n'
